-- Banking Management System - Database Initialization
-- Tables are auto-created by Hibernate (ddl-auto: update) in each microservice.
-- This script is for reference / manual setup and seed data only.

CREATE DATABASE bankdb;

-- Example seed (optional, run manually after services have created their tables):
-- INSERT INTO users (username, email, password, role, enabled, created_at)
-- VALUES ('admin', 'admin@bank.com', '$2a$10$replace_with_bcrypt_hash', 'ADMIN', true, now());
